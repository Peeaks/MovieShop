@model Admin.Views.PromoCodes.PromoCodeDeleteViewModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>PromoCode</h4>
    <hr />
    
    @if (Model.ShitWentWrong) {
        <p>This promo code is attached to atleast one order, please disable it instead of deleting</p>
    }

    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PromoCode.Code)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PromoCode.Code)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.PromoCode.Discount)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PromoCode.Discount)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
